apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  labels:
    app: employees
    component: webapp
spec:
  type: LoadBalancer  
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: employees
      